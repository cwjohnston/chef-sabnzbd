---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-12.04
    run_list:
      - recipe[apt]
  - name: ubuntu-14.04
    run_list:
      - recipe[apt]

suites:
  - name: default
    run_list:
      - recipe[sabnzbd::default]
    attributes:
  - name: runit
    run_list:
      - recipe[sabnzbd::default]
    attributes:
      sabnzbd:
        init_style: runit
